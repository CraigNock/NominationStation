{
  "name": "nominationstationserver",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "start": "ts-node server.ts",
    "heroku-postbuild": "cd client && yarn && yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --clear --respawn --transpile-only server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.6.4",
    "express": "^4.17.1",
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0-pre.62",
    "typescript": "^4.0.2"
  }
}
